--- a/ogr/ogrsf_frmts/dxf/ogrdxflayer.cpp	2026-02-09 19:36:29 +0200
+++ b/ogr/ogrsf_frmts/dxf/ogrdxflayer.cpp	2026-02-27 18:07:15 +0200
@@ -1026,6 +1026,8 @@
     double dfX = 0.0;
     double dfY = 0.0;
     double dfZ = 0.0;
+    //JU: Append rotation parameter
+    double dfAngle = 0.0;
     bool bHaveZ = false;
 
     while ((nCode = poDS->ReadValue(szLineBuf, sizeof(szLineBuf))) > 0)
@@ -1045,6 +1047,11 @@
                 bHaveZ = true;
                 break;
 
+            //JU: Append rotation parameter
+            case 50:
+                dfAngle = CPLAtof(szLineBuf);
+                break;
+
             default:
                 TranslateGenericProperty(poFeature.get(), nCode, szLineBuf);
                 break;
@@ -1067,8 +1074,31 @@
 
     poFeature->SetGeometryDirectly(poGeom);
 
+    //JU: Hack point symbol rotation
     // Set style pen color
-    PrepareLineStyle(poFeature.get());
+    //PrepareLineStyle(poFeature.get());
+
+
+    /* -------------------------------------------------------------------- */
+    /*      Format the style string.                                        */
+    /* -------------------------------------------------------------------- */
+
+    CPLString osStyle;
+    char szBuffer[64];
+
+    // Font name
+    osStyle.Printf("SYMBOL(c:");
+    osStyle += poFeature->GetColor(poDS);
+
+    if (dfAngle != 0.0)
+    {
+        CPLsnprintf(szBuffer, sizeof(szBuffer), "%.3g", dfAngle);
+        osStyle += CPLString().Printf(",a:%s", szBuffer);
+    }
+
+    osStyle += ")";
+
+    poFeature->SetStyleString(osStyle);
 
     return poFeature.release();
 }
